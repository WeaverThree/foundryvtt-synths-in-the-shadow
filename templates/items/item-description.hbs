<section class="description">
  {{#if editable}} 
    <prose-mirror name="system.description" data-document-uuid="{{uuid}}" value="{{system.description}}" collaborate="true" toggled="true">
      {{{enrichedDescription}}}
    </prose-mirror>
  {{else}} {{{enrichedDescription}}} {{/if}}
</section>